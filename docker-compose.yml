services:
  backend:
    container_name: backend
    build: 
      context: ./Backend
    ports:
      - "8000:8000"
  frontend:
    container_name: frontend
    build:
      context: ./Frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend